# Frontend environment variables
# Copy this file to .env and adjust as needed.

# Base URL for the backend API used by the API client.
# If omitted, the client will default to '/api' (useful when CRA proxy is configured).
REACT_APP_API_BASE=http://localhost:8000
